---
phase: 07-english-translation-pass
plan: 06
type: verify
wave: 5
depends_on: [07-03, 07-04, 07-05]
files_modified: []
autonomous: true
requirements:
  - TRANS-02
  - TRANS-03
  - TRANS-06
---

<objective>
Verify that the entire English translation pass is complete: every user-visible string in the app comes from i18n namespaces, the English experience is identical to pre-Phase-7, and the build passes cleanly.

Purpose: This is the Phase 7 gate check. No code changes — only verification.
Output: Verification report confirming Phase 7 success criteria.
</objective>

<execution_context>
@C:/Users/Emil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Emil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-english-translation-pass/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Phase 7 verification</name>
  <files></files>
  <action>
Run the following verification checks:

**Build & Test:**
1. `npm run build` — must succeed with zero errors
2. `npm run test` — must pass all tests
3. `npm run lint` — must pass (or only show pre-existing warnings)

**Namespace completeness:**
4. Verify all 5 namespace files exist and are valid JSON:
   - `public/locales/en/common.json`
   - `public/locales/en/landing.json`
   - `public/locales/en/steps.json`
   - `public/locales/en/generating.json`
   - `public/locales/en/report.json`

**Hardcoded string audit:**
5. Search for remaining hardcoded English strings in page/component files. Acceptable exceptions:
   - `toOptions()` arrays (scoring keys — intentionally not translated)
   - CSS class strings, color values, HTML tags
   - `console.log` / `console.error` messages
   - URL strings, localStorage keys, technical identifiers
   - `generateMockReport()` template content (explicitly out of scope per context doc)
   - NotFound.tsx (explicitly skipped per context doc)
   - Error boundary text (explicitly skipped)
   Everything else should use t() calls.

6. Specifically verify zero hardcoded strings remain in:
   - `src/pages/Index.tsx`
   - `src/pages/AuditForm.tsx`
   - `src/pages/Loading.tsx`
   - `src/pages/Report.tsx`
   - `src/components/audit/Step1BusinessInfo.tsx` through `Step8Financial.tsx`
   - `src/components/audit/AuditFormComponents.tsx`

**Functional verification (via `npm run dev`):**
7. Landing page: all hero text, benefits, stats, niche cards, how-it-works, footer render correctly
8. Audit form: step labels, nav buttons, validation errors, save toast, niche badge all render
9. Steps 1-8: all field labels, placeholders, hints, section headers render for both HS and RE
10. Loading page: animated steps cycle, heading, subtitle display
11. Report page: all sections, scores, labels, CTA, error states render
12. Full flow: Landing → Select HS → Steps 1-8 → Generate → Report — no broken text

**Success criteria verification against ROADMAP.md Phase 7:**
13. "All 8 form step labels, descriptions, placeholders, and validation messages are served from en/steps.json via t() calls — no hardcoded English strings remain in step components" — VERIFY
14. "The landing page, audit form, and report page render identically to v1.0 in English (no visual or behavioral regressions)" — VERIFY
15. "The report page headings, section labels, and score display text are served from en/report.json" — VERIFY
  </action>
  <verify>
All checks pass. Document results.
  </verify>
  <done>Phase 7 verification complete. All success criteria met.</done>
</task>

</tasks>

<verification>
This plan IS the verification — results documented in the summary.
</verification>

<success_criteria>
- Build passes with zero errors
- Tests pass
- No hardcoded English strings remain in page/component files (with documented exceptions)
- All 5 namespace JSON files are valid and complete
- All 3 Phase 7 success criteria from ROADMAP.md are met
- The English experience is identical to pre-Phase-7
</success_criteria>

<output>
After completion, create `.planning/phases/07-english-translation-pass/07-06-SUMMARY.md` (verification report)
</output>
