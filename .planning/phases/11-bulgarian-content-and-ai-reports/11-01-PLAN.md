---
phase: 11-bulgarian-content-and-ai-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/locales/bg/common.json
  - public/locales/bg/landing.json
  - public/locales/bg/steps.json
  - public/locales/bg/generating.json
  - public/locales/bg/report.json
autonomous: true
requirements: [TRANS-01]

must_haves:
  truths:
    - "User visiting /bg/ sees the landing page entirely in Bulgarian"
    - "User sees all form step labels, titles, subtitles, hints, and placeholders in Bulgarian"
    - "User sees the generating page text in Bulgarian"
    - "User sees the report page headings and labels in Bulgarian"
    - "No English strings appear on any /bg/ page (except brand name 'E&P Systems' and technical terms like CRM, KPI)"
  artifacts:
    - path: "public/locales/bg/common.json"
      provides: "Bulgarian common UI strings (nav, buttons, validation, niche labels, step labels, rating labels)"
      contains: "brand"
    - path: "public/locales/bg/landing.json"
      provides: "Bulgarian landing page content (hero, benefits, stats, niche select, how it works)"
      contains: "hero"
    - path: "public/locales/bg/steps.json"
      provides: "Bulgarian form step labels, field labels, hints, placeholders for all 8 steps"
      contains: "step1"
    - path: "public/locales/bg/generating.json"
      provides: "Bulgarian generating page strings (heading, steps, errors, rate limit messages)"
      contains: "heading"
    - path: "public/locales/bg/report.json"
      provides: "Bulgarian report page strings (hero, scores, sections, CTA, errors)"
      contains: "hero"
  key_links:
    - from: "public/locales/bg/*.json"
      to: "public/locales/en/*.json"
      via: "Identical key structure — every key in en/*.json must exist in bg/*.json"
      pattern: "same nested JSON key structure"
---

<objective>
Populate all 5 Bulgarian translation files with complete Bulgarian content matching the English JSON structure key-for-key.

Purpose: Without populated Bulgarian translation files, the /bg/ experience falls back to English for every string due to i18next's fallbackLng: 'en'. This plan delivers the core Bulgarian UI content that makes the /bg/ path actually work in Bulgarian.

Output: 5 fully populated bg/*.json files covering common, landing, steps, generating, and report namespaces.
</objective>

<execution_context>
@C:/Users/Emil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Emil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bulgarian-content-and-ai-reports/11-CONTEXT.md
@.planning/phases/11-bulgarian-content-and-ai-reports/11-RESEARCH.md

# Source files — English translations to match structure
@public/locales/en/common.json
@public/locales/en/landing.json
@public/locales/en/steps.json
@public/locales/en/generating.json
@public/locales/en/report.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate bg/common.json, bg/landing.json, bg/generating.json</name>
  <files>public/locales/bg/common.json, public/locales/bg/landing.json, public/locales/bg/generating.json</files>
  <action>
Read the corresponding English files (en/common.json, en/landing.json, en/generating.json) and create complete Bulgarian translations with identical key structures.

**Translation guidelines (from CONTEXT.md):**
- AI/Claude translation is acceptable for all strings
- Use formal business tone with formal address (Вие) throughout
- Brand name "E&P Systems" stays in English
- Business terms like CRM, KPI, ROI remain in English as they are internationally used
- Use Bulgarian conventions throughout

**Specific files:**

1. **bg/common.json** (~65 lines): Translate all buttons, validation messages, niche labels, step labels, rating labels, form placeholders. Keep `brand.name` = "E&P Systems" and `brand.initials` = "E&P". Translate niche labels: "Home Services & Trades" -> suitable Bulgarian, "Real Estate Teams & Brokerages" -> suitable Bulgarian.

2. **bg/landing.json** (~68 lines): Translate the full landing page including hero section, benefits array (5 items), stats, niche selection cards (titles, descriptions, tags, CTA), and how-it-works section (3 steps). Keep the same messaging structure. "FREE · AI-POWERED · PERSONALIZED" badge should be translated. Tags in niche cards should be Bulgarian equivalents of trade names.

3. **bg/generating.json** (~25 lines): Translate the 8 loading steps array, heading, subtitle, error messages, and rate limit messages. For rate limit section, add new keys that will be used by the machine-readable error code pattern:
   - Keep existing keys (`rateLimit.heading`, `rateLimit.savedMessage`, `rateLimit.defaultMessage`)
   - Add new keys: `rateLimit.message` (with `{{timeHint}}` interpolation), `rateLimit.timeHints.oneHour`, `rateLimit.timeHints.hours` (with `{{count}}` interpolation), `rateLimit.timeHints.tomorrow`

**IMPORTANT:** Every key present in the English file MUST have a corresponding key in the Bulgarian file. No keys should be missing.
  </action>
  <verify>
Verify all 3 files are valid JSON: use `node -e "JSON.parse(require('fs').readFileSync('public/locales/bg/common.json','utf8'))"` and similarly for landing.json and generating.json. Compare key counts: run `node -e "const en=require('./public/locales/en/common.json');const bg=require('./public/locales/bg/common.json');const enKeys=JSON.stringify(Object.keys(en).sort());const bgKeys=JSON.stringify(Object.keys(bg).sort());console.log('Match:',enKeys===bgKeys)"` for each file (top-level keys).
  </verify>
  <done>
bg/common.json, bg/landing.json, and bg/generating.json contain complete Bulgarian translations with every key from the corresponding English file present. All files parse as valid JSON.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate bg/steps.json and bg/report.json</name>
  <files>public/locales/bg/steps.json, public/locales/bg/report.json</files>
  <action>
Read en/steps.json (225 lines) and en/report.json (88 lines) and create complete Bulgarian translations.

**bg/steps.json guidelines:**
- Translate all 8 steps: titles, subtitles, field labels, hints, placeholders
- This is the largest file — translate systematically step by step
- For hs/re conditional sections, translate both branches
- Placeholders (e.g., "ABC Plumbing & Heating") should use a Bulgarian-appropriate example (e.g., "Петров ВиК ЕООД")
- Email placeholder: use a Bulgarian-looking example (e.g., "иван@петроввик.бг" or keep generic "ivan@example.com")
- Phone placeholder: use Bulgarian format (e.g., "+359 88 123 4567")
- Sub-niche labels (subNiche.hvac, etc.) MUST be translated — these are the display labels for the sub-niche selector cards. Use the BG_SUB_NICHE_LABELS from research: ОВК, ВиК, Електроинсталации, Гаражни врати, DDD/Дезинфекция, Озеленяване, Професионално почистване, Покриви, Бояджийски услуги, Строително предприемачество, Строителство, Интериорен дизайн, Жилищни продажби, Търговски имоти, Управление на имоти, Ново строителство, Луксозни имоти
- The sub-niche title questions (step1.hs.subNiche.title, step1.re.subNiche.title) must be translated
- Keep the same structure of nesting (step1.fields.businessName.label, step1.hs.fields.industry.label, etc.)

**bg/report.json guidelines:**
- Translate all report page strings: hero section, score labels, benchmark labels, section headings, CTA section, error messages, share button text
- For executiveSummaryTemplate: translate all template strings, keep `{{interpolation}}` variables intact and HTML tags like `<strong>`, `<scoreStrong>` unchanged
- Category names: translate all 7 categories (Technology & Software -> Технологии и софтуер, etc.)
- Score labels: Strong -> Силен, Moderate -> Умерен, Needs Work -> Нуждае се от подобрение, Critical Gap -> Критичен пропуск
- Benchmark labels: Above Average -> Над средното, Average -> Средно, Below Average -> Под средното

**IMPORTANT:** Every key present in the English file MUST have a corresponding key in the Bulgarian file. Verify no keys are missing.
  </action>
  <verify>
Verify valid JSON: `node -e "JSON.parse(require('fs').readFileSync('public/locales/bg/steps.json','utf8'))"` and similarly for report.json. Do a deep key comparison to ensure structural parity with English files.
  </verify>
  <done>
bg/steps.json and bg/report.json contain complete Bulgarian translations with every key from the corresponding English files present. All files parse as valid JSON. The full /bg/ experience has UI strings for all pages.
  </done>
</task>

</tasks>

<verification>
1. All 5 bg/*.json files parse as valid JSON without errors
2. Every top-level and nested key in en/*.json has a corresponding key in bg/*.json (structural parity)
3. No English text leaks through (except brand name "E&P Systems", technical terms like CRM/KPI, and interpolation variables)
4. `npm run build` passes without errors
</verification>

<success_criteria>
- 5 Bulgarian translation files fully populated (previously all empty `{}`)
- Key structure matches English counterparts exactly
- Build passes
- A user visiting /bg/ would see Bulgarian text for all UI elements (form options handled separately in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/11-bulgarian-content-and-ai-reports/11-01-SUMMARY.md`
</output>
